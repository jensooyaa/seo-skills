# Skills-SEO-Doctor

> 面向 FocusWork 的部门 Skill，前端方向。@黄鑫
>
> 目录名：`seo-doctor`
> 安装路径：`C:/Users/huangxin0510/.focuswork/users/huangxin0510/skills-custom/seo-doctor/`

## 一、定位

**总指挥型 Skill** —— 不自己造轮子，负责决定测什么、调用合适的工具、把各家输出统一解读，产出开发能直接行动的报告。

- **用户**：前端 / 测试 / 运营，不需要懂 SEO
- **场景**：转测前自测、上线前卡口、上线后效果分析
- **原则**：确定性规则交给脚本执行以保证稳定，模型负责编排、语义判断与报告生成
- **不绑业务线**：规范取自公开标准（Google Search Essentials、schema.org、W3C）

命名取「医生」之意：查症状（页面规范）、看化验单（GSC 数据）、对比片子（前后 diff）。

---

## 二、功能范围

| # | 功能 | 一句话 |
|---|---|---|
| 1 | **单页规范检查** | 给一个 URL，查语义化标签、HTML 元信息、结构化数据 |
| 2 | **全站体检** | 爬全站 + 对比 sitemap，批量跑检查，找孤儿页和漏提交 |
| 3 | **上线前后 diff** | 测试环境 vs 线上，看这次改动有没有把 SEO 改坏 |
| 4 | **GSC 数据分析** | 操控浏览器进 Search Console 导出数据，找可行动结论 |
| 5 | **报告生成** | 分级 + 四段式，面向不懂 SEO 的开发 |

### 明确不做

| 不做 | 原因 |
|---|---|
| 性能与 Core Web Vitals | Lighthouse 的主场，做不过它 |
| 修复代码自动生成 | 报告给「怎么改」的文字说明即可，不替开发写代码 |
| 关键词研究 / 外链 / SERP 排名 | 需要 Ahrefs / Semrush 级别的付费数据源 |
| SSR / CSR 差异对比 | 判定标准难调、误报率高，暂不纳入 |
| AI 搜索可见性（GEO） | 结果不稳定需多次采样，留作二期 |
| 内置任何业务线的规范 | 通用工具，不绑业务 |

---

## 三、技术选型

总指挥定位下，重、专业、规则常变的部分用现成库；轻且直接决定报告质量的部分自己写。

| 能力 | 谁做 | 用什么 |
|---|---|---|
| 编排调度 | 🔴 自己做 | `SKILL.md`，**核心资产** |
| 语义化 / 元信息检查 | 🔴 自己做（轻） | `cheerio` + 几十行 |
| 全站爬虫 | 🔴 自己做（轻） | 见 M3 说明 |
| 上线前后 diff | 🔴 自己做 | 跑两遍单页检查做对比 |
| 输出统一层 + 报告生成 | 🔴 自己做 | 面向开发的四段式 |
| **浏览器操作** | 🟢 **平台自带** | **FocusWork 内置 `browser` 工具，Agent 直接调，不进 run.js** |
| 结构化数据校验 | 🟢 现成 | `@adobe/structured-data-validator` |
| robots.txt 匹配 | 🟢 现成 | `robots-parser` |
| sitemap 解析 | 🟢 现成 | `sitemapper` |
| CSV 解析 | 🟢 现成 | `papaparse` |
| 规则表 / 提示词 | 🟢 抄改 | `claude-seo`（MIT 协议） |

npm 依赖共 4 个，体积都很轻。

**不挂任何外部 MCP** —— 每多一个 MCP 就多一道安装门槛，和「零配置、装上即用」的定位冲突。

### 平台带来的分工线

FocusWork 的浏览器是内置 `browser` 工具，由 Agent 直接调用，**不经过 Skill**。因此：

| | 归谁 |
|---|---|
| 打开页面、点击、输入、snapshot | **Agent 调 `browser` 工具** |
| GSC 导航步骤 | **写成 `references/gsc-navigation.md` 的自然语言步骤**，不是代码 |
| HTTP 请求、HTML 解析、文件处理、CSV 解析 | **run.js** |

### 为什么语义化检查不用现成工具

这部分用 `cheerio` 几十行就能写完，但**建议自己写**，两个理由：

1. **误报可控** —— 用现成工具，它报什么你收什么，想调阈值、想去掉误报高的规则，做不到
2. **输出可控** —— 我们要的是「哪个元素、怎么改」，现成工具给的是「SEO score 68 分」

这部分是报告的骨架，不该外包。

---

## 四、模块拆解

按依赖关系排列，可逐个实施。

### M0 · 平台能力实测（阻塞项，先做）

结论会改变后面的架构，**跳过直接写代码有返工风险**。

| 要确认 | 影响 |
|---|---|
| ~~skill 能否执行 `npm i`~~ | ✅ **已确认支持** —— 规范写明「有依赖时在 `<skill-name>/` 目录内执行 npm install」 |
| `references/` 能否被 Agent 按需读取 | 能 → 规则表外置，SKILL.md 精简；不能 → 规则表内联，SKILL.md 会很长 |
| `browser` 有没有下载类 action、下载文件落在哪 | 决定 M5 走哪条方案 |
| `snapshot` 对表格页能读出什么 | M5 的退路之一 |

详见第九节 P0。

---

### M1 · 单页检查引擎 ⭐ 地基

其他所有模块都依赖它。

- **输入**：一个 URL
- **输出**：统一格式的问题列表 JSON

#### 四组检查

| 组 | 内容 | 用什么 |
|---|---|---|
| **语义化** | `div` / `button` + onClick 当跳转、空 href、img 缺 alt、h1 缺失或多个、标题层级跳级、有无 `<main>` / `<nav>` / `<header>` / `<footer>` | `cheerio` |
| **元信息** | title / description 缺失与长度、meta robots 误挂 noindex、canonical 缺失或指向他页、lang、viewport、og / twitter 卡片 | `cheerio` |
| **结构化数据** | ld+json 提取、JSON 语法、schema.org 类型与属性、Google 富媒体必填字段 | `@adobe/structured-data-validator` |
| **索引指令** | 该 URL 有没有被 robots.txt 误屏蔽 | `robots-parser` |

#### 关键设计：输出统一层

四组检查来源不同，必须归一成同一种结构：

```json
{
  "rule":   "h1-missing",
  "level":  "阻断",
  "target": "页面",
  "detail": "该页面没有 h1 标题",
  "howto":  "在正文最顶部加一个描述页面核心内容的 h1"
}
```

后面所有模块吃的都是这个结构，这层做扎实了后面全省事。

#### 已确认的两处收敛

- **语义化只查唯一性标签**（`main` / `nav` / `header` / `footer`），**不查「正文有没有包 article」** —— 脚本判定不了正文在哪，会大量误报
- **不查「面包屑与可见面包屑一致」** —— 定位不到可见面包屑，不可靠

**工作量**：约 300 行 · 难度 ★☆☆

---

### M2 · 报告生成

- **输入**：M1 的 JSON
- **输出**：HTML 报告

结构：

- **顶部**：阻断 / 警告 / 建议三级计数
- **每条问题四段式**：

```
[阻断] 该页面没有 h1 标题
  是什么   页面中 <h1> 数量为 0
  为什么   搜索引擎用 h1 判断页面主题，缺失会显著影响主题识别
  怎么改   在正文最顶部加一个描述页面核心内容的 h1
  影响面   抽样 20 页命中 12 页，推算全站约 60%
```

「怎么改」是**文字说明**，不生成代码片段。只给「缺少 canonical」这种规则名，对不懂 SEO 的开发等于没说。

**M1 + M2 完成即第一个可交付节点**，到这里就有能给人看的东西了。

**工作量**：模板 + 提示词 · 难度 ★☆☆

---

### M3 · 全站体检

- **输入**：站点根 URL
- **输出**：全站问题分布 + 孤儿页 + 漏提交清单

#### 两条腿走路

这是全站体检的核心价值 —— 不只是「批量跑一遍单页检查」：

```
sitemap 清单        ←→        爬虫实际爬到的页面
（声明要收录的）                （爬虫真能到达的）
              ↓ 对比
  ├─ sitemap 有、爬不到    → 孤儿页，爬虫进不去
  ├─ 爬得到、sitemap 没有  → 漏提交
  └─ 两边都有              → 正常，批量跑 M1 检查
```

#### 爬虫自己写，不用 site-audit-seo

三个理由：

1. 只需要「发现 URL + 抓 HTML」，不需要它的评分体系
2. 要边爬边跑 M1 检查，用现成的得多一层数据搬运
3. 它强绑 Lighthouse，每页都跑一遍会非常慢，而性能我们不做

#### 必须做的三件事

| 项 | 说明 |
|---|---|
| 控制并发 | 别把测试环境打挂 |
| 遵守 robots | 用 `robots-parser` 判断能不能爬 |
| 限制深度与总量 | 默认封顶（建议 500 页），可显式放大 |

**工作量**：约 200 行 · 难度 ★★☆

---

### M4 · 上线前后 diff

- **输入**：两个 URL（测试环境 / 线上）
- **输出**：SEO 元素变化对照表

```
本次改动前后，该页面 SEO 元素变化：

  ✅ title         无变化
  ⚠️ description   156 字符 → 空
  ❌ canonical     /products/abc → 消失
  ⚠️ ld+json       Product 类型丢失 offers 字段
  ✅ h1            无变化
```

实现就是跑两遍 M1、结果做 diff。

**最便宜的一个模块，但可能是同事最常用的** —— 开发不会主动做体检，但会主动确认自己没改坏东西。这条也最容易接进 CI。

**工作量**：约 80 行 · 难度 ★☆☆

---

### M5 · GSC 数据分析

唯一的真技术难点，也是最有价值的一块。

#### 链路

```
Agent 调 browser 工具 → Search Console → 选资源和时间范围
  → 点「导出」下载 CSV → run.js 读取并用 papaparse 解析
  → 模型分析 → 报告
```

#### 关键：走导出，不抓 DOM

| 做法 | 评价 |
|---|---|
| ❌ 抓 GSC 页面 DOM | 重 JS 的 SPA，表格虚拟滚动、图表是 canvas 读不出数值，一改版全废 |
| ✅ **点「导出」下 CSV** | 每个报告都有官方导出功能，数据完整，不受改版影响 |
| ⭕ 接 Search Console API | 最稳，但要建 GCP 项目、配 OAuth，**二期再说** |

用用户已登录的浏览器会话，不需要处理登录、不需要 API key、不花钱，却能拿到最权威的数据。

#### 三条备选方案（取决于 P0-4 实测结果）

| 方案 | 做法 | 前提 / 风险 |
|---|---|---|
| **A（首选）** | `profile: "user"` 打开 GSC → 点导出 → run.js 扫描用户下载目录取最新 CSV | 需用户授权本机浏览器；要按修改时间 + 文件名匹配识别「刚下的那个」 |
| **B** | 用 `snapshot` 直接读 GSC 表格 | 不确定能否拿到表格数值；虚拟滚动可能只读到当前视口 |
| **C** | 改走 Search Console API（`googleapis`） | 最稳但要 GCP 项目 + OAuth，门槛高 |

#### 拉哪些数据

| 数据源 | 内容 |
|---|---|
| 效果报告 | 点击 / 曝光 / CTR / 平均排名，可按查询、页面、国家、设备拆 |
| 网页收录状态 | 已编入索引 vs 未编入索引，**及未收录的原因分类**（重复网页未选定 canonical、已抓取尚未编入索引、已发现尚未抓取、软 404、被 robots 屏蔽） |
| 站点地图 | 提交状态、已发现 URL 数 |
| 增强报告 | 面包屑、FAQ、产品等结构化数据的有效 / 警告 / 错误 |

#### 模型要产出的是结论，不是搬运数据

- **高曝光低点击的页面** → title / description 没吸引力
- **排名 11~20 的查询** → 临门一脚，推一把就能上第一页的机会词
- **零曝光页面占比** → 说明选词有问题，不是页面质量问题
- **收录失败原因聚类** → 哪些是模板层问题（会波及全站），哪些是个例
- **交叉分析** → GSC 说这批页没收录，丢给 M1 实测，一路追到代码根因

#### 前提、风险与边界

| 项 | 说明 |
|---|---|
| 前提 | 用户 Chrome 已登录 Google 账号，且对该 GSC 资源有访问权限 |
| 风险 | 界面改版会让导航步骤失效 → **把界面操作步骤单独抽成 `references/gsc-navigation.md`**，坏了能快速改 |
| 风险 | 控制操作频率，别短时间反复点 |
| 边界 | **只读 + 导出，不做任何写操作**（不提交 sitemap、不请求编入索引） |

**工作量**：约 150 行 + 大量调试 · 难度 ★★☆

---

## 五、目录结构

按 FocusWork 自定义 Skill 规范：`SKILL.md` 必须，`run.js` 为唯一入口，`package.json` 声明依赖。

```
C:/Users/huangxin0510/.focuswork/users/huangxin0510/skills-custom/
└── seo-doctor/
    ├── SKILL.md                  ← 必须，name 必须等于目录名
    ├── package.json              ← 4 个依赖
    ├── run.js                    ← 唯一入口，--mode 分发
    ├── lib/
    │   ├── check-page.js         # M1 单页检查
    │   ├── crawl-site.js         # M3 全站爬取
    │   ├── diff-pages.js         # M4 前后对比
    │   ├── parse-gsc.js          # M5 CSV 解析
    │   └── report.js             # M2 报告生成
    └── references/
        ├── rules-semantic.md     # 语义化规则表
        ├── rules-meta.md         # 元信息规则表
        ├── rules-schema.md       # 结构化数据规则表
        ├── gsc-navigation.md     # ⭐ GSC 界面操作步骤，给 Agent 看
        └── report-template.md    # 报告模板
```

### SKILL.md frontmatter

```yaml
---
name: seo-doctor
description: "检测网页 SEO 规范：语义化标签、HTML 元信息、结构化数据；支持全站体检、改动前后对比、Search Console 数据分析。Use when: 用户要检查网页或网站的 SEO、查 meta 标签是否规范、验证结构化数据、找孤儿页、对比上线前后 SEO 变化、分析 Search Console 数据。NOT for: 页面性能与 Core Web Vitals 检测、关键词研究、外链分析、排名查询、直接修改代码。"
metadata:
  openclaw:
    emoji: "🩺"
    requires:
      bins: [node]
---
```

`NOT for:` 里把已砍掉的功能明确列出，避免 Agent 误匹配。

### 调用方式

```bash
node "C:/Users/huangxin0510/.focuswork/users/huangxin0510/skills-custom/seo-doctor/run.js" --mode check --url "https://xxx.com/p/123"
```

**路径一律用绝对路径**（规范硬要求）。

---

## 六、可参考的资产

| 来源 | 参考什么 |
|---|---|
| [AgriciDaniel/claude-seo](https://github.com/AgriciDaniel/claude-seo) | MIT 协议，25 个 sub-skill 的规则定义，直接对照可省掉大半规则表工作量 |
| [Google Search Central](https://developers.google.com/search/docs) | 富媒体结果字段要求，规则表的权威来源 |
| schema.org 官方 JSON-LD 词汇表 | 全量类型与属性定义 |
| 王尊 `Skills-Web-Test` | 浏览器操作的实践经验（平台自带 browser 工具，链路本身不用复用） |
| 本仓库 `src/docs/` 的四份 SEO 文档 | 作为**验证数据集** —— 有已知答案的真实样本，用于测召回率与误报率 |

---

## 七、待确认

| # | 事项 | 何时确认 |
|---|---|---|
| 1 | `references/` 能否被 Agent 按需读取 | P0-3 |
| 2 | `browser` 能否拿到下载的文件 | P0-4，决定 M5 走 A / B / C |
| 3 | 全站爬取默认封顶页数 | 建议 500 页，可显式放大 |
| 4 | 报告落盘位置 | 生成到当前目录，还是固定路径 |

---

## 八、实施优先级清单

每一条都可独立验证，做完一条确认通过再做下一条。

### P0 · 平台能力实测（先做，不写业务代码）

**结论会改变后面的架构。跳过 P0 直接写代码有返工风险。**

| # | 任务 | 怎么验证 | 谁做 | 耗时 |
|---|---|---|---|---|
| **P0-1** | 建空壳 skill：`seo-doctor/` + SKILL.md（只有 frontmatter + 一句 Usage）+ run.js（只 `console.log`） | 对 Agent 说「用 seo-doctor 检查 https://xxx.com」，看**能不能匹配到并调起 run.js** | 你 | 20min |
| **P0-2** | 在 skill 目录 `npm i cheerio`，run.js 里 require 并解析一段 HTML | 能跑通，不报模块找不到 | 你 | 15min |
| **P0-3** | 建 `references/test.md`，SKILL.md 里写「详细规则见 references/test.md」 | 看 Agent **会不会主动去读那个文件** | 你 | 15min |
| **P0-4** | 试 `browser` 下载能力：① 有没有 download 类 action ② `profile:"user"` 下载的文件落到哪 ③ `snapshot` 对表格页能读出什么 | 记录三个答案 | 你 | 30min |

> **P0-3 的影响**：能读 → 规则表放 `references/`，SKILL.md 保持精简；不能读 → 规则表内联，SKILL.md 会很长，可能要拆多个 skill。
>
> **P0-4 的影响**：能拿到下载文件 → M5 走方案 A；不能 → 退到 B（snapshot）或 C（GSC API）。

### P1 · M1 单页检查引擎

| # | 任务 | 产出 | 怎么验证 | 耗时 |
|---|---|---|---|---|
| **P1-1** | 写语义化规则表（约 8 条） | `references/rules-semantic.md` | 通读一遍，判定条件与报告文案是否说得清 | 1h |
| **P1-2** | `run.js` + `lib/check-page.js`：抓 HTML → cheerio → 跑语义化规则 → 输出统一 JSON | 能跑的脚本 | **拿已知有问题的页面跑**，看该报的报没报、不该报的有没有误报 | 2h |
| **P1-3** | 加元信息组 | 规则表 + 代码 | 重点验 canonical 的 URL 归一化（尾斜杠、大小写、协议） | 1.5h |
| **P1-4** | 接 `@adobe/structured-data-validator` | 规则表 + 代码 | **拿 SourcingX 关键词页跑，和 `validator.schema.org` 对比**，看覆盖度够不够 | 2h |
| **P1-5** | 接 `robots-parser` | 代码 | 找一个 robots.txt 里 Disallow 的路径试 | 30min |

> 验证数据集用自家站 —— `src/docs/` 里有已知答案（如那 226 个缺 canonical 的页面），最适合测召回率与误报率。
>
> **P1-2 做完就有第一个能跑的东西了。**

### P2 · M2 报告生成

| # | 任务 | 怎么验证 | 耗时 |
|---|---|---|---|
| **P2-1** | 报告模板 + 生成逻辑：三级计数 + 四段式 | **找一个不懂 SEO 的同事看报告，问「你知道该改什么吗」** —— 答不上来就是模板没写好 | 2h |

验收标准不是技术性的，是**可读性**。别自己判断，一定找人看。

**到这里是第一个完整可交付节点** —— 能演示、能给人用。

### P3 · M4 上线前后 diff

| # | 任务 | 怎么验证 | 耗时 |
|---|---|---|---|
| **P3-1** | 跑两遍 M1，结果做对比，输出变化表 | **故意在测试环境删掉一个 canonical / 改掉 meta**，看能不能准确报出来 | 1h |

最便宜，性价比最高，排在全站之前。

### P4 · M3 全站体检

| # | 任务 | 怎么验证 | 耗时 |
|---|---|---|---|
| **P4-1** | 接 `sitemapper` 解析 sitemap | 条数对不对、嵌套 sitemapindex 能不能展开 | 1h |
| **P4-2** | 写爬虫：并发控制 + 遵守 robots + 深度和总量封顶 | 对测试环境跑，看会不会打挂、封顶生不生效 | 2h |
| **P4-3** | 爬取结果 vs sitemap 对比，输出孤儿页 / 漏提交 | **人工确认几个报出来的孤儿页是不是真的没有入链** | 1h |

### P5 · M5 GSC 数据分析

**依赖 P0-4 结论，方案定了再开始。**

| # | 任务 | 怎么验证 | 耗时 |
|---|---|---|---|
| **P5-1** | 写 `references/gsc-navigation.md`：GSC 界面操作步骤 | Agent 照着能不能走通全流程 | 1h |
| **P5-2** | 拿到 CSV → `papaparse` 解析 | 数据条数、字段和 GSC 界面上看到的一致 | 1h |
| **P5-3** | 分析逻辑：高曝光低点击、排名 11-20、收录失败聚类 | **结论要能人工复核** —— 随机抽 5 条去 GSC 里对一遍 | 3h |
| **P5-4** | 交叉分析：GSC 报的问题页 → 丢给 M1 实测 → 找根因 | 拿那 226 个缺 canonical 的页面试，能不能自动串起来 | 2h |

**P5-4 是整个 skill 的演示主线**，做出来汇报效果最好。

### 总览

```
P0 平台实测（1.5h）        ← 先做
  ↓
P1 单页检查（5 步，7h）
  ↓
P2 报告生成（2h）          ← 第一个可交付
  ↓
P3 前后 diff（1h）         ← 最便宜
  ↓
P4 全站体检（4h）
  ↓
P5 GSC 分析（7h）          ← 最难，演示主线
```

合计约 22 小时纯开发，加调试与试用，四周排期够。

---

## 附：部门文档格式

按其他同事的条目体例撰写，可直接复制进部门文档。

### 【背景】

- 前端页面的 SEO 问题上线时看不出来，往往几个月后才从 Search Console 里发现，返工成本高。

- 开发同学在转测前想自测 SEO，缺少低门槛工具：专业 SEO 软件需付费安装、配置门槛高，输出的又是给 SEO 专员看的评分报告，开发看不出该改哪里。

- 上线后的效果数据在 Google Search Console 里，需要人工逐个报表翻看和导出，既费时又难和代码问题对应起来。

- SEO 规范散落在各处，靠人眼逐页抽检，既不稳定也无法沉淀为团队可复用的检查标准。

### 【目标】

- 提升前端转测前的 SEO 自测能力，让不懂 SEO 的开发同学也能在上线前完成检查。

- 基于任意 URL，自动完成页面规范检测、全站体检、改动前后对比与分级报告输出。

- 打通「线上数据异常 → 页面实测 → 代码根因」的分析链路，让 Search Console 的数据能直接落到具体改动上。

- 沉淀可复用的 SEO 检查规范，支持各业务线持续补充。

### 【解决方案】

基于 Focus Work 内置 AI Agent、子 Agent 与浏览器操作能力，形成「范围确认 → 页面采样 → 语义化与元信息检查 → 结构化数据校验 → 全站爬取与 sitemap 比对 → 改动前后对比 → Search Console 数据导出与归因 → 分级报告输出 → 检查规范沉淀」的 SEO 检测 Skill。

Skill 定位为总指挥：确定性规则调用成熟开源库执行以保证稳定性，浏览器操作交由平台内置 browser 工具，模型负责编排调度、语义判断、跨数据源归因与报告生成。规范知识底座取自 Google Search Essentials、schema.org、W3C 无障碍标准等公开权威标准，不绑定任何业务线。

### 【计划】

> * [ ] **9月4日：确定技术架构方案和实施方向，完成平台能力前置验证。**
>
> * [ ] **9月5日-9月18日**：**完成开发，调试优化检测效果**
>
> * [ ] **9月19日-9月30日**：**找各业务线开发试用，调优误报率并完善**
>
